name: Emacs 29

on:
  push:
    branches:
      - master
      - dev
    paths:
      - 'Formula/emacs-head@29.rb'
      - 'patches/**'
      - 'Library/**'
      - '.github/workflows/emacs-29.yml'
  pull_request:
    paths:
      - 'Formula/emacs-head@29.rb'
      - 'patches/**'
      - 'Library/**'
      - '.github/workflows/emacs-29.yml'
  schedule:
    - cron:  '0 7 * * *'

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-15]
        build_opts:
          # All options (imagemagick@7) + native-comp support
          - '--with-cocoa --with-no-frame-refocus --with-imagemagick --with-pdumper --with-xwidgets --with-tree-sitter --with-native-comp --with-retro-icon-sink-bw'

    env:
      HOMEBREW_EMACS_HEAD_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_EMACS_HEAD_GITHUB_REPOSITORY_REF: ${{ github.head_ref || github.ref }}

    steps:
    - uses: actions/checkout@v5

    - name: Build emacs-head@29 ${{ matrix.build_opts }}
      run: |
        brew update
        brew upgrade || true
        export HOMEBREW_DEVELOPER=1
        brew install emacs-head@29.rb ${{ matrix.build_opts }} --verbose
      working-directory: Formula

    - name: Test installation
      run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
      working-directory: Formula
