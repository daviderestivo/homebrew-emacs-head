name: Emacs (default)

on:
  push:
    branches:
      - master
      - dev
    paths:
      - 'Aliases/emacs-head'
      - '.github/workflows/emacs.yml'
  pull_request:
    paths:
      - 'Aliases/emacs-head'
      - '.github/workflows/emacs.yml'
  schedule:
    - cron:  '0 7 * * *'

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-15]
        build_opts:
          # All options (imagemagick@7)
          - '--with-cocoa --with-no-frame-refocus --with-imagemagick --with-pdumper --with-xwidgets'

    env:
      HOMEBREW_EMACS_HEAD_GITHUB_REPOSITORY: ${{ github.repository }}
      HOMEBREW_EMACS_HEAD_GITHUB_REPOSITORY_REF: ${{ github.head_ref || github.ref }}

    steps:
    - uses: actions/checkout@v5

    - name: Build emacs-head ${{ matrix.build_opts }}
      run: |
        brew update
        brew upgrade || true
        export HOMEBREW_DEVELOPER=1
        brew install ./Aliases/$(readlink Aliases/emacs-head) ${{ matrix.build_opts }} --verbose

    - name: Test installation
      run: $(brew --prefix)/bin/emacs --batch --eval='(print (+ 2 2))'
      working-directory: Formula
